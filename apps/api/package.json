{
	"name": "@miforge/api",
	"private": true,
	"version": "0.0.1",
	"main": "src/index.ts",
	"type": "module",
	"scripts": {
		"build": "tsdown && pnpm run build:types && pnpm run build:bun",
		"build:types": "tsc --emitDeclarationOnly",
		"build:bun": "bun build --compile --minify --sourcemap ./src/index.ts --outfile dist/server",
		"dev": "bun run --tsconfig-override ./tsconfig.dev.json --inspect=localhost:6499/ --hot src/index.ts",
		"db:generate": "prisma generate --no-hints",
		"db:migrate": "prisma migrate deploy",
		"db:migrate:dev": "prisma migrate dev",
		"db:seed": "bun run prisma/seed/index.ts",
		"db:reset": "prisma migrate reset --force",
		"types": "tsc -b",
		"check": "biome check .",
		"lint": "biome lint .",
		"prepare": "pnpm run db:generate && pnpm run build && pnpm run build:types"
	},
	"dependencies": {
		"@orpc/client": "^1.10.0",
		"@orpc/json-schema": "^1.10.0",
		"@orpc/openapi": "^1.10.0",
		"@orpc/server": "^1.10.0",
		"@orpc/zod": "^1.10.0",
		"@prisma/adapter-mariadb": "^6.15.0",
		"@prisma/client": "^6.15.0",
		"dotenv-flow": "^4.1.0",
		"envalid": "^8.1.0",
		"hono": "^4.10.3",
		"jsonwebtoken": "^9.0.2",
		"reflect-metadata": "^0.2.2",
		"tsyringe": "^4.10.0",
		"zod": "^4.1.12"
	},
	"devDependencies": {
		"@types/bun": "^1.3.0",
		"@types/jsonwebtoken": "^9.0.10",
		"prisma": "^6.15.0",
		"prisma-dbml-generator": "^0.12.0",
		"tsdown": "^0.15.9",
		"typescript": "^5.9.3"
	},
	"packageManager": "pnpm@10.18.3"
}
